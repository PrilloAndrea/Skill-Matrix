table:
  name: users
  schema: public
array_relationships:
  - name: answers
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: answers
          schema: public
  - name: boards_admins
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: boards_admins
          schema: public
